
// ViewState enum defines the available views in the application for type-safe navigation.
export enum ViewState {
  LOADING = 'LOADING',
  LOGIN = 'LOGIN',
  HOME = 'HOME',
  CHAPTERS = 'CHAPTERS',
  READER = 'READER',
}
